/*
 * Definimos las variables fadeIn y fadeOut usando las transiciones que queremos
 *
 * Hay que notar aca que estamos creando aca la secuencia de la transición fadeIn
 * pero fadeOut solo guardamos la referencia al recurso ya que lo instanciaremos cuando
 * inicie la habitación
 */
//fadeIn = layer_sequence_create("Fade", room_width / 2, room_height / 2, FadeIn);
//fadeOut = FadeOut;